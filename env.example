#!/bin/bash
# change as needed and save as .env file
DB_NAME=db
DB_USER=postgres
DB_PASS=postgres
DB_HOST=localhost
DB_PORT=5432
export DB_URL="postgresql+asyncpg://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}"